# Practice - Candidate Pipeline Dashboard

## 1. מטרת התרגול

הפרויקט נבנה כתרגול פיתוח עם Claude Code, על בסיס מסמך אפיון כתוב (`SPEC.md`).
מטרת התרגול: לתת ל-Claude Code לבנות גרסה ראשונה ועובדת של Dashboard לניהול לידים ומועמדים, כאשר `SPEC.md` הוא מקור האמת לדרישות.

## 2. הפרומפט הראשוני שניתן ל-Claude Code

```text
Read SPEC.md carefully.

Your task is to build the first working version of the project described in SPEC.md.

Before writing code:
1. Briefly summarize the project requirements.
2. Propose the file structure you will create.
3. Identify any assumptions you need to make.

Then build the project.

Important:
- Follow SPEC.md as the source of truth.
- Keep the implementation simple and suitable for GitHub Pages.
- Use HTML, CSS and JavaScript only unless there is a strong reason not to.
- Use Hebrew RTL correctly.
- Use localStorage for persistence.
- Create clear, readable code.
- Do not add unnecessary frameworks or dependencies.

After implementation:
- Verify that the main requirements in SPEC.md are covered.
- Tell me which files were created.
- Tell me how to run and test the project locally.
```

## 3. מה Claude Code יצר

| קובץ | תפקיד |
|---|---|
| `index.html` | מבנה הדף: כרטיסי סיכום, אזור חיפוש וסינון, טבלת הלידים וטופס הוספה/עריכה (חלון `dialog`). |
| `css/style.css` | עיצוב, תצוגת RTL, צבע לכל סטטוס, הבלטת Follow-up להיום ובאיחור, והתאמה למובייל. |
| `js/app.js` | הלוגיקה: נתוני דוגמה, שמירה ב-`localStorage`, חישוב כרטיסי הסיכום, חיפוש, סינון, מיון, הוספה, עריכה ומחיקה. |
| `README.md` | הסבר על הפרויקט, הוראות הרצה ובדיקה מקומית, הכללים שנקבעו והוראות פרסום ב-GitHub Pages. |

`SPEC.md` היה קיים בתיקייה לפני תחילת העבודה, ולא שונה.

## 4. תהליך העבודה

1. **קריאת `SPEC.md`**: Claude Code קרא את האפיון. לפני כתיבת הקוד הוא הציג סיכום דרישות, מבנה קבצים מוצע (לפי סעיף 15 באפיון) ורשימת הנחות.
2. **יצירת האפליקציה**: נוצרו הקבצים `index.html`, `css/style.css` ו-`js/app.js`.
3. **פתיחה ובדיקה בדפדפן**: בניסיון הראשון הקובץ נפתח ישירות מהדיסק בדפדפן המובנה, והוצג בלי CSS ו-JavaScript (מגבלה של תצוגת הקבצים בדפדפן המובנה). לכן הופעל שרת סטטי זמני ב-PowerShell, והאתר נבדק דרך `http://localhost`. במחשב לא היו מותקנים Python או Node.js, ולכן השתמשנו ב-PowerShell. השרת נוצר בתיקייה זמנית, שימש לבדיקות בלבד, לא נוסף לפרויקט והופסק בסוף העבודה.
4. **בדיקת תצוגת מחשב**: ברוחב 1400px הטבלה חרגה מהמסגרת. המדידה הראתה שרוחב התוכן 1465px לעומת 1336px זמינים, ועמודת "פעולות" נחתכה.
5. **התאמת רוחב הטבלה**: הוקטנו הריווח בתאים וגודל הגופן בטבלה, קוצרה עמודת ההערה, והוגדר רוחב מינימלי לעמודת "פעולה הבאה". בבדיקה חוזרת הטבלה נכנסה במלואה, בלי גלילה אופקית.
6. **בדיקת תצוגת מובייל**: ברוחב 375px הטבלה מוצגת ככרטיסים נפרדים לכל ליד, ואין גלילה אופקית בדף.
7. **תיקון כפתורי עריכה/מחיקה במובייל**: בכרטיס הליד במובייל הכפתורים "עריכה" ו"מחיקה" נדחקו לשני קצוות הכרטיס. ה-CSS תוקן כך שהם מוצגים יחד.
8. **בדיקת לוגיקת Follow-up**: בוצעה בדיקה אוטומטית ב-JavaScript בתוך הדפדפן. נבדקו ספירת "Follow-up נדרש", הסימון "היום" ו"באיחור", והסינונים.
9. **מציאת חוסר עקביות ותיקונו**: במהלך הבדיקה נמצא שליד בסטטוס "נסגר בהצלחה" עם תאריך Follow-up שעבר הופיע בסינון "באיחור", למרות שבטבלה הוא לא מסומן כבאיחור. הסינונים "באיחור" ו"היום" תוקנו כך שהם כוללים רק לידים פתוחים, כלומר לא "נסגר בהצלחה" ולא "לא רלוונטי", בדיוק כמו ההבלטה בטבלה.
10. **בדיקת טופס העריכה**: הטופס נפתח עם כל פרטי הליד הקיימים.
11. **יצירת `README.md`**: נכתבו הוראות הרצה, בדיקה ופרסום ב-GitHub Pages.

## 5. החלטות והנחות שנעשו במהלך הפיתוח

האפיון לא הגדיר את ההתנהגויות הבאות במדויק, ולכן נקבעו הכללים האלה:

- **Follow-up נדרש**: ליד שתאריך ה-Follow-up שלו הוא היום או שכבר עבר. לידים בסטטוס "נסגר בהצלחה" או "לא רלוונטי" לא נספרים.
- **לידים בתהליך**: הסטטוסים נוצר קשר, שיחה התקיימה, Follow-up ומתלבט. "פגישה נקבעה" ו"נסגר בהצלחה" מקבלים כרטיסי סיכום משלהם.
- **מיון ברירת מחדל לפי דחיפות**: Follow-up באיחור, אחריו היום, אחריו עתידי, ואחריו לידים ללא תאריך. לידים סגורים או לא רלוונטיים מוצגים בסוף. ההחלטה נועדה לתת מענה לעיקרון המרכזי באפיון (סעיף 19): "עם מי אני צריך לדבר עכשיו".
- **תאריכי נתוני הדוגמה**: נקבעים יחסית ליום ההפעלה הראשונה, כך שבהתחלה תמיד יש לידים עם Follow-up להיום ולידים באיחור.

## 6. בדיקות שבוצעו

| בדיקה | מה נבדק | תוצאה |
|---|---|---|
| תצוגת מחשב | צילום מסך ברוחב 1400px ומדידת רוחב הטבלה | ✅ אחרי התאמת הרוחב |
| תצוגת מובייל | צילום מסך ברוחב 375px, בדיקה שאין גלילה אופקית | ✅ אחרי תיקון הכפתורים |
| RTL | כיוון הדף, הטבלה והטופס בצילומי המסך | ✅ |
| כרטיסי סיכום | הערכים בטעינה הראשונה (12 / 2 / 7 / 5 / 1 / 1) ועדכונם אחרי הוספה ועריכה | ✅ |
| Follow-up היום/באיחור | תגיות "היום" ו"באיחור" בטבלה, והסינון "באיחור" | ✅ אחרי תיקון הסינון |
| חיפוש וסינון | חיפוש לפי חלק ממספר טלפון ולפי אימייל, סינון לפי רמת עניין ולפי Follow-up, לחיצה על כרטיס "Follow-up נדרש", כפתור "נקה סינונים" | ✅ |
| הוספת ליד | שמירה בלי שם נחסמת; ליד עם שם נשמר ומופיע בטבלה | ✅ |
| עריכת ליד | הטופס נפתח עם הנתונים הקיימים; שינוי שם וסטטוס נשמר ומעדכן את הכרטיסים | ✅ |
| מחיקת ליד | הליד נמחק מהטבלה ומ-`localStorage` (בבדיקה האוטומטית חלון האישור אושר מראש בקוד) | ✅ |
| שמירה ב-`localStorage` | ליד שנוסף ונערך נשאר אחרי רענון הדף | ✅ |

הבדיקות בוצעו בדפדפן המובנה של Claude Code, חלקן בצילומי מסך וחלקן בהרצת JavaScript בתוך הדף.

## 7. תוספות מעבר ל-SPEC

תוספות קטנות שלא נדרשו באפיון:

- לחיצה על כרטיס סיכום מסננת את הטבלה בהתאם.
- מספרי טלפון וכתובות אימייל בטבלה ניתנים ללחיצה (`tel:` / `mailto:`).
- כפתור "איפוס לנתוני דוגמה" בתחתית הדף, עם הודעת אישור.

## 8. שלב נוסף: ייבוא וייצוא CSV

### הפרומפט שניתן ל-Claude Code

```text
I want to add CSV import/export support to the current project.

Please add:

1. Export to CSV
- Add a button in the UI: "ייצוא ל-CSV"
- Export all current leads to a UTF-8 CSV file.
- Include all lead fields.
- Make sure Hebrew text is preserved correctly when opening the CSV in Excel.
- Use clear column headers in Hebrew.

2. Import from CSV
- Add a button in the UI: "ייבוא מ-CSV"
- Allow the user to select a CSV file.
- Validate the file structure before importing.
- Do not crash if some optional fields are missing.
- Require at least the lead name field.
- Show a clear message if the file is invalid.
- Ask for confirmation before adding imported leads.
- Imported leads should be saved into localStorage and immediately appear in the dashboard.

3. Keep the existing application behavior unchanged.
4. Do not add external libraries unless absolutely necessary.
5. Keep the solution compatible with GitHub Pages.
6. Update README.md to document CSV import/export.
7. Update Practice.md with this additional development step and the tests performed.

After implementation:
- Test export and import.
- Verify Hebrew text survives export/import correctly.
- Tell me exactly which files were changed.
```

### מה נעשה

- **קובץ חדש `js/csv.js`**: פונקציות כלליות לכתיבה ולקריאה של CSV, בלי ספריות חיצוניות. הפונקציות תומכות בשדות בתוך מרכאות, בפסיקים ובירידות שורה בתוך שדה, ובמרכאות כפולות. המפריד (פסיק או נקודה-פסיק) מזוהה אוטומטית.
- **`js/app.js`**: נוסף חלק שממיר לידים לשורות CSV ובחזרה. הוא כולל את כותרות העמודות בעברית, בדיקת תקינות הקובץ, הודעות שגיאה, הודעת אישור לפני הייבוא ושמירה ב-`localStorage`.
- **`index.html`**: נוספו הכפתורים "ייצוא ל-CSV" ו"ייבוא מ-CSV" בראש הדף, שדה בחירת קובץ מוסתר, וטעינה של `js/csv.js`.
- **`css/style.css`**: עיצוב לקבוצת הכפתורים בראש הדף, כולל פריסה במובייל.
- **`README.md`**: נוסף סעיף שמתעד את הייבוא והייצוא.

### החלטות בשלב הזה

- הקובץ מיוצא ב-UTF-8 עם BOM בתחילתו, כדי ש-Excel יזהה את הקידוד ויציג עברית תקינה.
- סטטוס ורמת עניין מיוצאים כטקסט בעברית, ותאריכים בפורמט `YYYY-MM-DD`.
- המזהה הפנימי של הליד לא מיוצא. בייבוא נוצר לכל ליד מזהה חדש, והלידים **מתווספים** לקיימים. אין החלפה ואין בדיקת כפילויות.
- הייצוא כולל את כל הלידים, גם כשמופעל סינון בטבלה.
- בייבוא רק עמודת "שם" היא חובה. שורה בלי שם מדולגת. סטטוס, רמת עניין או תאריך לא תקינים מקבלים ערך ברירת מחדל, ומוצגת הערה בהודעת האישור.
- תאריכים בייבוא מתקבלים בפורמט `YYYY-MM-DD` וגם `DD/MM/YYYY` או `DD.MM.YYYY`, כי Excel שומר לפעמים בפורמט הזה.
- ערך שמתחיל ב-`=`, `+`, `-` או `@` מקבל גרש בתחילתו בייצוא, כדי ש-Excel לא יריץ אותו כנוסחה. מספרי טלפון לא משתנים, והגרש מוסר בייבוא.

### בדיקות שבוצעו בשלב הזה

הבדיקות בוצעו בדפדפן המובנה של Claude Code דרך שרת סטטי זמני ב-PowerShell (אותו שרת מהשלב הקודם, שוב מחוץ לפרויקט). רוב הבדיקות רצו כ-JavaScript בתוך הדף:
- הקובץ המיוצא נלכד בזיכרון, במקום הורדה אמיתית.
- בחירת הקובץ לייבוא נעשתה בקוד, באמצעות `DataTransfer` על שדה הקובץ, במקום דרך חלון בחירת קבצים.
- חלונות האישור וההודעות (`confirm` / `alert`) הוחלפו בקוד כדי לקרוא את הטקסט שלהם.

| בדיקה | תוצאה |
|---|---|
| ייצוא: שם הקובץ `leads-2026-09-22.csv`, סוג `text/csv;charset=utf-8`, שלושת הבייטים הראשונים `EF BB BF` (BOM) | ✅ |
| ייצוא: שורת כותרות בעברית ו-12 שורות נתונים. הערה שמכילה פסיק נעטפה במרכאות | ✅ |
| ייבוא חוזר של הקובץ המיוצא: 12 לידים נוספו (12→24), **כל 10 השדות זהים למקור, כולל כל הטקסט בעברית**, והמזהים ייחודיים | ✅ |
| הלידים המיובאים מופיעים מיד בטבלה ובכרטיסי הסיכום, ונשמרים ב-`localStorage` | ✅ |
| קובץ בלי עמודת "שם", קובץ ריק, קובץ עם כותרות בלבד, קובץ בסיומת שאינה `.csv`: מוצגת הודעת שגיאה ברורה, ושום דבר לא מיובא | ✅ |
| קובץ עם עמודת "שם" בלבד: הלידים מיובאים, והשדות האחרים ריקים או בברירת מחדל | ✅ |
| שורה עם טלפון ובלי שם: מדולגת, ומספר השורה מופיע בהודעת האישור | ✅ |
| קובץ בסגנון Excel (BOM, נקודה-פסיק, ירידות שורה של Windows, תאריכים `05/10/2026` ו-`1.9.26`) | ✅ |
| שדות במרכאות, עם פסיק, ירידת שורה ומרכאות כפולות בתוך השדה | ✅ |
| סטטוס, רמת עניין ותאריך לא תקינים (`31/02/2026`): ערכי ברירת מחדל והערות בהודעת האישור | ✅ |
| כותרות באנגלית, ועמודה לא מוכרת (`CRM ID`) שמדווחת כעמודה שלא יובאה | ✅ |
| ביטול בהודעת האישור: שום דבר לא נשמר | ✅ |
| הגנה מנוסחאות: `=SUM(1+1)` ו-`@mention` מקבלים גרש, `+972-50-1234567` ו-`-5` לא משתנים, והכל חוזר לערך המקורי בייבוא | ✅ |
| התנהגות קיימת: כרטיסי הסיכום, חיפוש, הוספה ומחיקה עובדים כמו קודם. הטבלה ברוחב 1400px עדיין בלי גלילה אופקית. במובייל (375px) אין גלילה אופקית והכפתורים החדשים מסודרים | ✅ |

במהלך הבדיקות תוקן ניסוח בעברית: "נמצאו 1 לידים" ו"1 שורות ללא שם" שונו לצורת יחיד ("נמצא ליד אחד", "שורה אחת ללא שם").

**מה לא נבדק:** הקובץ לא נפתח בפועל ב-Excel, כי אין Excel בסביבת הבדיקה. הקידוד נבדק לפי ה-BOM ולפי ייבוא חוזר של הקובץ. מומלץ לפתוח קובץ מיוצא ב-Excel ולוודא ידנית שהעברית מוצגת נכון.

## 9. Git ו-GitHub

### 9.1 קובץ `.gitignore`

לפני יצירת ה-repository נוצר בתיקיית הפרויקט קובץ `.gitignore` עם הכללים: `exports/`, `*.tmp`, `.DS_Store`, `Thumbs.db`.

### 9.2 יצירת Git repository מקומי

- `git init -b main`: ה-repository נוצר בתיקיית הפרויקט, והענף הראשי נקרא `main`.
- לפני ההוספה ל-staging בוצעה בדיקה עם `git status`. נמצאו 8 קבצים של הפרויקט, ואף קובץ CSV לא נמצא בתיקייה.
- בדיקה עם `git check-ignore` אישרה שהתיקייה `exports/` וקבצים בתוכה לא נכללים ב-repository.
- שם המשתמש והאימייל של Git כבר היו מוגדרים מראש, ולא שונו.

### 9.3 Commit ראשון

- הקבצים שנוספו: `.gitignore`, `Practice.md`, `README.md`, `SPEC.md`, `css/style.css`, `index.html`, `js/app.js`, `js/csv.js`.
- הודעת ה-commit: `Initial working version of Candidate Pipeline Dashboard` (commit `bf22926`).

### 9.4 יצירת repository ב-GitHub

- נבדק שחשבון GitHub CLI המחובר הוא `roee-aido`, ושעדיין אין בחשבון repository בשם הזה.
- ה-repository נוצר כ-Public עם `gh repo create`. GitHub לא יצר README, `.gitignore` או רישיון, כי הקבצים האלה כבר היו קיימים מקומית.
- כתובת ה-repository: https://github.com/roee-aido/Candidate-Pipeline-Dashboard

### 9.5 Push ל-main

- ה-repository המקומי חובר ל-GitHub כ-remote בשם `origin`.
- הענף `main` נדחף ל-GitHub. נבדק שה-commit האחרון ב-GitHub (`bf22926`) זהה ל-commit המקומי.

### 9.6 פרסום ב-GitHub Pages

- GitHub Pages הופעל דרך ה-API של GitHub (`gh api`). הפרסום הוא מהענף `main`, מתיקיית השורש (`/`).
- לא נדרש שינוי בקוד האפליקציה. הפרויקט מורכב מקבצים סטטיים בלבד עם נתיבים יחסיים, ולכן עובד כמו שהוא.
- ה-build הראשון הסתיים בהצלחה (סטטוס `built`, בלי שגיאות).
- בדיקות על האתר החי:
  - `index.html`, `css/style.css`, `js/csv.js` ו-`js/app.js` נטענים עם HTTP 200.
  - האתר נפתח בדפדפן המובנה של Claude Code. ה-CSS וה-JavaScript נטענו, 12 לידי הדוגמה ו-6 כרטיסי הסיכום הוצגו, ולא היו שגיאות ב-Console.
  - הדף מוצג בעברית ובכיוון RTL: `dir="rtl"` ו-`lang="he"`, הכותרת בצד ימין, והעמודה "שם" היא הימנית בטבלה.
  - הכפתור "ייצוא ל-CSV" יצר קובץ UTF-8 עם BOM ו-13 שורות (כותרות ו-12 לידים).
  - הכפתור "ייבוא מ-CSV" פותח את חלון בחירת הקובץ. ייבוא חוזר של הקובץ שיוצא הוסיף 12 לידים, וכל השדות בעברית נשמרו בלי שינוי.
  - בדיקת הייבוא והייצוא בוצעה בהרצת JavaScript בדף, באותה שיטה כמו בסעיף 8. בסיום נוקו נתוני הבדיקה מהדפדפן.

### 9.7 כתובת האתר הסופית

**https://roee-aido.github.io/Candidate-Pipeline-Dashboard/**

## 10. סטטוס נוכחי

גרסה 1 של ה-Dashboard, כולל ייבוא וייצוא CSV, נמצאת ב-GitHub ומפורסמת ב-GitHub Pages:
https://roee-aido.github.io/Candidate-Pipeline-Dashboard/
